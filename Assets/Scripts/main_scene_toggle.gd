extends Node
# ● Stringapplication/run/main_scene [default: ""]
##	Path to the main scene file that will be loaded when the project runs.

#@onready var main_scene : String = .GLOBAL_GET("application/run/main_scene")
#@onready var main_scene_path : String = "res://%s" % main_scene
#@onready var main_scene_instance : Node = load(main_scene_path).instantiate()

#func _ready() -> void:
#	print("Main scene path: %s" % main_scene)


